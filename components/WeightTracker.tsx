'use client'

import WidgetFrame from './WidgetFrame'

// Шаг 5 по плану: живой трекер веса с localStorage и недельным средним.
export default function WeightTracker() {
  return (
    <WidgetFrame label="Трекер веса">
      <p style={{ margin: 0, color: 'var(--mute)' }}>Инструмент в разработке.</p>
    </WidgetFrame>
  )
}
